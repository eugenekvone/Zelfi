export const getAllActivity__items = (state) => {
	return state.activity.allActivity.items;
};

export const getRecommendActivity__items = (state) => {
	return state.activity.recommendActivity.items;
};

